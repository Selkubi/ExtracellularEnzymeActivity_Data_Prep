% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/significant_p_value_extractor.R
\name{significant_p_value_extractor}
\alias{significant_p_value_extractor}
\title{Extract Significant Pairwise Comparisons from an emmeans Object}
\usage{
significant_p_value_extractor(emmeans_object)
}
\arguments{
\item{emmeans_object}{An object of class \code{emmGrid} returned by the \code{emmeans} function
with pairwise contrasts. Typically, this object is obtained
by using the \code{emmeans} function with the \code{pairwise} option.}
}
\value{
A \code{data.table} containing only the rows where the p-value is less than 0.05.
The returned table includes the significant comparisons and their associated p-values.
}
\description{
This function filters pairwise comparison results from an \code{emmeans} object to return only
the comparisons with a p-value less than 0.05, indicating statistical significance.
}
\details{
This function converts the provided \code{emmeans} object to a \code{data.table}
and filters for rows where \code{p.value < 0.05}. This is useful for identifying
statistically significant differences in pairwise comparisons.
}
\examples{
# Assuming `model` is a fitted model (e.g., from `lmer()`),
# and `emmeans_object` contains pairwise comparisons:
library(emmeans)
model <- lmer(response ~ fixed_effects + (1 | random_effect), data = your_data)
emmeans_object <- emmeans(model, pairwise ~ your_factor)$contrasts

# Extract significant pairs
significant_pairs <- significant_p_value_extractor(emmeans_object)

}
