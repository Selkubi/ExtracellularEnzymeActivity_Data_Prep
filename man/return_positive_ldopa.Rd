% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_L_DOPA.R
\name{return_positive_ldopa}
\alias{return_positive_ldopa}
\title{Filter Positive L-DOPA Measurements Based on Abiotic Correction
This function takes a data frame of L-DOPA measurements and returns only the positive values, applying a threshold of zero. Negative values are replaced with \code{NA}. It assumes that the first five columns contain the relevant measurements.}
\usage{
return_positive_ldopa(data)
}
\arguments{
\item{data}{A data frame containing L-DOPA measurements. The first five columns are expected to contain the replicate data.}
}
\value{
A data frame with the same structure as the input, where only positive values are retained in the first five columns. Negative values are replaced with \code{NA}.
}
\description{
Filter Positive L-DOPA Measurements Based on Abiotic Correction
This function takes a data frame of L-DOPA measurements and returns only the positive values, applying a threshold of zero. Negative values are replaced with \code{NA}. It assumes that the first five columns contain the relevant measurements.
}
\examples{
# Example data frame with some negative values
example_data <- data.frame(rep1 = c(-0.1, 0.5, 0.3),
                           rep2 = c(0.2, -0.4, 0.1),
                           rep3 = c(0.6, 0.7, -0.2),
                           rep4 = c(0.1, 0.3, -0.1),
                           rep5 = c(-0.5, 0.8, 0.2))

# Apply the function
return_positive_ldopa(example_data)

}
